--- /c/Users/vasum/VB_champsim_Optimised_Hermesed/champsim_v17/inc/lpm_tracker.h	2026-06-09 23:51:11.000000000 +0900
+++ /c/Users/vasum/VB_champsim_Optimised_Hermesed/champsim_v17_D2/inc/lpm_tracker.h	2026-06-19 02:27:53.992840600 +0900
@@ -492,27 +492,42 @@
 /*-----------------------------------------------------------------
  * lpm_operate() — call from CACHE::operate() BEFORE handle_*
  *-----------------------------------------------------------------*/
-inline void lpm_operate(int cpu, uint8_t cache_type,
+/* ── D2: overload taking the already-resolved tracker reference T.
+ *    Eliminates the `lpm[cpu][cache_type]` recompute (imul 0x2c0) at each use.
+ *    T MUST be bound to exactly lpm[cpu][cache_type] by the caller; cpu/cache_type
+ *    are still required because the body uses cache_type for branch selection and
+ *    cpu to reach the independent cross-tracker lpm[cpu][LPM_L1D]. Logic is
+ *    byte-identical to the index-based version below. ── */
+inline void lpm_operate(LPM_Tracker& T, int cpu, uint8_t cache_type,
                         bool hit_active, bool miss_active,
                         uint64_t α, bool has_byp_miss,
                         uint64_t load_α, uint64_t load_miss) {
+    (void)cpu;
 #ifdef BYPASS_LLC_LOGIC
     if (cache_type == IS_LLC) {
-        lpm[cpu][cache_type].tick_llc_byp(hit_active, miss_active, has_byp_miss);
+        T.tick_llc_byp(hit_active, miss_active, has_byp_miss);
     } else
 #endif
 #ifdef BYPASS_L1_LOGIC
     if (cache_type == IS_L2C) {
         bool l1d_pm = (lpm[cpu][LPM_L1D].last_class == LPM_CLASS_M);
-        lpm[cpu][cache_type].tick_byp(hit_active, miss_active, has_byp_miss, l1d_pm);
+        T.tick_byp(hit_active, miss_active, has_byp_miss, l1d_pm);
     } else
 #endif
     {
-        lpm[cpu][cache_type].tick(hit_active, miss_active);
+        T.tick(hit_active, miss_active);
     }
 
     /* HOT path: only tick α/load deltas. Divs deferred to demandCompute. */
-    lpm[cpu][cache_type].tick_alphas(α, load_α, load_miss);
+    T.tick_alphas(α, load_α, load_miss);
+}
+
+inline void lpm_operate(int cpu, uint8_t cache_type,
+                        bool hit_active, bool miss_active,
+                        uint64_t α, bool has_byp_miss,
+                        uint64_t load_α, uint64_t load_miss) {
+    lpm_operate(lpm[cpu][cache_type], cpu, cache_type,
+                hit_active, miss_active, α, has_byp_miss, load_α, load_miss);
 }
 
 /*-----------------------------------------------------------------
--- /c/Users/vasum/VB_champsim_Optimised_Hermesed/champsim_v17/src/cache.cc	2026-06-09 23:51:11.000000000 +0900
+++ /c/Users/vasum/VB_champsim_Optimised_Hermesed/champsim_v17_D2/src/cache.cc	2026-06-19 02:28:06.615439700 +0900
@@ -1005,7 +1005,7 @@
                 uint64_t α_c = __lsc.alpha_total;
                 uint64_t load_α_c = __lsc.load_alpha;
                 uint64_t load_miss_c = __lsc.load_miss;
-                lpm_operate(c, cache_type, hit_active, miss_active, α_c, has_byp, load_α_c, load_miss_c);
+                lpm_operate(lpm[c][cache_type], c, cache_type, hit_active, miss_active, α_c, has_byp, load_α_c, load_miss_c);
             }
 #else
             /* LLC per-core — bucket MSHR state by entry.cpu, then tick each CPU separately */
@@ -1057,11 +1057,11 @@
                 uint64_t load_α_c = __lsc.load_alpha;
                 uint64_t load_miss_c = __lsc.load_miss;
                 bool hit_active_c = (RQ.occupancy | WQ.occupancy) > 0;
-                lpm_operate(c, cache_type, hit_active_c, miss_active_pc[c], α_c, has_byp_pc[c], load_α_c, load_miss_c);
+                lpm_operate(lpm[c][cache_type], c, cache_type, hit_active_c, miss_active_pc[c], α_c, has_byp_pc[c], load_α_c, load_miss_c);
             }
 #endif
         } else {
-            lpm_operate(cpu, cache_type, hit_active, miss_active,
+            lpm_operate(lpm[cpu][cache_type], cpu, cache_type, hit_active, miss_active,
                         α, has_byp, load_α, load_miss);
         }
     }
