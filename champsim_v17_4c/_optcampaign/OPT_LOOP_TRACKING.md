# OPT LOOP TRACKING (append-only)

Frozen base = `champsim_v18_TEST_OPT` @ git `0295747` (NEVER edit — only `cp`-duplicate).
**GATE:** execution_checksum MUST == `140038115682937`; wall ≤ `216.7s` (×5 median only if a run looks slower).
**Workload:** `no spp no` + `--hermes ttp` + `--bypass 4000fix-KappaPhiL1L2`, perceptron/lru/glc, 8 cores, 2M warmup / 14M sim, trace `LLM256.Pythia-70M_21M`. Runner = `quickSim/run_one.ps1`.
All agents = Opus. Failed opts NEVER enter OPT_CATALOG.md.

## ROLLING STATUS BOARD (v19 → v20)  [update every turn]
**✅ DONE — committed v19 (`champsim_v19`, git 2355a08, ≈−4% paired: −3.85%..−4.33%):** Opt-A, P11, Opt-B, B2′ (reg_producers→flat ProducerList[256]).
**✅ KEPT since v19 (current checkpoint `champsim_v19_avx2_lpm`, ≈−16% cumulative vs frozen):** ★ -mavx2 -mbmi2 (−12.32%, checksum+IPC exact, %ymm=4395) → champsim_v19_avx2 · predictors#1 dead-lpm-cut (−0.34% flat, PROVEN dead via full-output diff) → champsim_v19_avx2_lpm. (candidate for v20 bump.)
**🛑 BUILD-FLAG THREAD CLOSED (per user 2026-06-20):** Makefile FROZEN at `-mavx2 -mbmi2` (the one accepted build baseline; binary was SSE-only/under-tuned). NO further flag tuning — -flto UNAVAILABLE (toolchain GCC built --disable-lto), -funroll/-mtune/PGO DROPPED (overtuning). Makefile is the FIXED build config; opts live in the SOURCE.
**🔄 IN-PROGRESS:** build lane FREE → next source opts (paired-gate checksum+IPC+FULL-DIFF). ALL research done (verdicts below). Branchless = DEAD VEIN (perceptron already branchless+AVX2-vec). scheduler-merge=FAILED(RAW). SoA-tag=never-proven(P3/P13 slower). P10=not-portable(AddressProxy live). mem_index_ring=DELETE(bitset scan supersedes the user's mem-only-scan idea). idle-batch=symmetric.
**⏸ TODO — SOURCE opts (gate checksum+IPC+full-diff each):** 1. get_set precompute NUM_SET_LOG2 (kill per-access log2 loop) · 2. DELETE dead mem_index_ring · 3. execute_store deepcopy→refs · 4. C1 DRAM incremental min-cursor (RISKY tie-break) · 5. Core#1 scheduler dead-zero limit→real cursor (RISKY stall-invariant). [const/restrict + extern-sweep = lower priority, source micro mostly noise-flat.]
**⏸ TODO — queued build opts (paired-gate each):**
  0. ★★ **`-mavx2 -mbmi2` build flags** (Makefile.win CXXFLAGS) — POTENTIALLY BIGGEST WIN. Binary is currently **SSE-only** (0 AVX2/512 in 578k-line disasm; -march=znver3 line is clang-only/inactive). Already-vectorized hot loops (CACHE/MEMORY_CONTROLLER/lpm/retire_rob stats+reductions, set.h join, schedule_instruction P15 fold) are stuck at 128-bit → AVX2 doubles them to 256-bit; also enables the ONE missed candidate (schedule_instruction cycle-ready compare→mask, ooo_cpu.cc:887). INTEGER SIMD = bit-exact → checksum-safe. 🚫 NOT -march=native (pulls -mfma → FP contraction → could flip perceptron/Hermes/bypass pred → break checksum). Paired-gate + HARD checksum-verify 140038115682937.
  1. ctz-expand — `set.h` 3 `expand()` variants → `__builtin_ctzll` set-bit walk. Confirmed checksum-exact (ascending order preserved), hot dep-merge path, -O3 can't. RUNNING.
  2. MAX_INSTR_DESTINATIONS extern-global → `NUM_INSTR_DESTINATIONS_SPARC` constexpr at 6 sites (ooo_cpu.cc:489/578/1226/2163/2170/2207). Unblocks -O3 unroll. checksum-exact. micro.
  3-7. **STAGED const-ref + __restrict + constexpr-const-fold campaign** (per hot file, one stage each, paired-gated). NOT blanket const (-O3 infers locals); only the reload-winning shapes: (a) const-REF pinning an element across an aliasing barrier [=P11], (b) constexpr-fold effective-constants [=MAX_INSTR_DESTINATIONS], (c) __restrict where no-alias PROVABLE (UB if wrong — checksum may NOT catch UB deterministically, so gate hardest + apply only to distinct arrays/objects). Stages by hotness: #3 bypass-model (ooo_l1/l2/llc_byp_model.cc, model ON = hottest), #4 LPM tracker, #5 spp prefetcher (l2c), #6 DRAM controller, #7 cache.cc (const-ref was FLAT there → only __restrict can help). Research each read-only first, then implement.
  8. extern-global-bound loop sweep (generalize MAX_INSTR_DESTINATIONS).
**🔬 RESEARCH DONE (read-only verdicts):** base+delta ROB/LQ/SQ compression = NO (addresses dominate, 128B=2 lines). fastset/bitset = YES→ctz-expand (#1). ROB instr-class ops = only MAX_INSTR_DESTINATIONS (#2); footprint NO. 32-bit words = NO (bitsets want 64; hot 64b fields need >32). PACKET (~256B/4 lines) = mostly NO: "90% empty" is data-dependent (one slot shared all types), NOT structural slack. Only safe wins = drop ~5 dead fields (~10-16B: pf_origin_level/data_index/is_producer-copy/prefetched/drc_tag_read) + union instruction_pa⟷data_pa (~8B) — but neither crosses a cache line → memory-only, no speed. Dep-set union UNSAFE (unconditional empty()/card reads + std::vector/POD/memcpy). rob_index⟷lq_index union IMPOSSIBLE (both live on data LOAD, ooo_cpu.cc:1939-1943). True compression = net loss (per-cycle queue scans).
**📦 16c-FOOTPRINT BATCH (deferred — needs a 16-core gate, NOT the 8c build lane; all M1-style risk: bitfield/narrow overhead may be 8c-flat/slower, pays only at 16c ×16 mem traffic):** producer_id `:34`+out-of-band has_producer bit (value is 34-bit; UINT64_MAX sentinel widens it) · M8 lq/sq_index→u8 (sentinel-migration) · M4 PACKET instruction_pa⟷data_pa union (~8B) + drop ~5 dead PACKET fields (~10-16B) · M5 RTE/RTL/RTS→u16 · M6/M7 BANK_REQUEST. ALL checksum-exact via sentinel-migration; gate at 16c.
**🚫 REJECTED (never re-add):** W3-E always_inline (+1.2%), M1+M3 lru-narrow (+1.1% at 8c), cache.cc hoists (flat/-O3-redundant), base+delta, 32-bit words, **B2'' svector (+1.94%, no struct shrink + deep-list thrash; B2' std::vector stays)**, ctz-expand (+0.43% flat — dep-sets stay in array mode, bitset branch cold).

Format — PASS: `vXX_YYYY | <≤16w opt> | checksum=… | IPC=… | wall=…s | PASS`
Format — FAIL: `vXX_YYYY FAIL AVG5 WORSE BY ZZ% — CONTINUE FROM <checkpoint> DIR TO DUP — cause:<why> — idea:<opt>`

**⚠ TIMING METHODOLOGY (revised 2026-06-20):** wall noise is ~4% (200.8–208.9s observed) and the
host DRIFTS — LARGER than per-opt effects (1–5s). Single-run gating vs a stale reference is invalid.
ALL wall gating now uses **paired interleaved** runs (candidate vs current checkpoint, alternating
×4–5, drop the first/warmup run) so drift cancels in the delta. checksum gate (architectural) is
unaffected and remains the hard correctness gate. Early single-run walls (v18_0000..0003) are
drift-confounded — being re-validated paired.

---

v18_0000 | BASELINE fork+14 (frozen ref) | checksum=140038115682937 | IPC=0.62178 | wall=216.7s | PASS (reference)
**VALIDATED CUMULATIVE (paired interleaved ×5, drop-warmup, drift-controlled, 2026-06-20):** frozen-v18 median **215.5s** vs 3-opt checkpoint (Opt-A+P11+Opt-B) median **209.0s** = **−6.5s / −3.0% REAL** (tight non-overlapping clusters; checksum 140038115682937 both). Opt-A+P11 carry it; Opt-B flat. (Same CKPT binary read 202.5s earlier vs 209.0s now → host drift ~6s → paired gating mandatory.)
v18_0001 | Opt-A working_bank_count (DRAM per-cycle bank scan -> O(1) counter, stats-only) | checksum=140038115682937 | IPC=0.62178 | wall=214.1s | PASS  [checkpoint dir: champsim_v18_optA]
v18_0002 | P11 O3 const-ref hoists (~10 hot fns in ooo_cpu.cc, reload elim) | checksum=140038115682937 | IPC=0.62178 | wall=209.4s | PASS  [checkpoint dir: champsim_v18_optA_P11]
v18_0003 | Opt-B DRAM idle-batch (accumulate idle cycles -> advance_idle(N), stats-only; advance_idle==N*tick proven exhaustively) | checksum=140038115682937 | IPC=0.62178 | wall=209.1s | PASS (flat — DRAM busy on this workload)  [checkpoint dir: champsim_v18_optA_P11_B]
v18_0004 FAIL AVG5 211.6s (median; 5 runs 211.0-212.0, checksum exact all 5) vs 209.1s — WORSE BY 1.2% — CONTINUE FROM champsim_v18_optA_P11_B DIR TO DUP — cause: V2 [[gnu::always_inline]] on per-cycle do_scheduling = i-cache bloat (R8 cc_now hoist neutral, R3 skipped) — idea: W3-E core micro-opts  [NOTE: not drift-controlled; but ×5 tight 211-212 + plausible cause → discard stands]
v18_0005 FAIL PAIRED +2.3s (interleaved ×4: median_M 204.8 vs median_B 202.5, drift-controlled, checksum exact all 8) — WORSE BY ~1.1% — CONTINUE FROM champsim_v18_optA_P11_B DIR TO DUP — cause: M1 lru u32->u8 = int-promotion overhead in hot LRU-update loop with NO real BLOCK shrink (alignment padding); M2 pmc skipped (unbounded accumulator); M3 drop-bypassed bundled — idea: M1+M3 footprint (defer: may still help 16c under a footprint-specific gate)
v18_0006 FAIL PAIRED +0.36% (median_C 202.4 vs median_K 201.7, interleaved ×4, overlapping clusters = within noise, NO benefit; checksum exact all 8) — CONTINUE FROM champsim_v18_optA_P11_B DIR TO DUP — cause: cache.cc const-ref hoists REDUNDANT with -O3 (compiler already eliminates these reloads; P11 only won because ooo_cpu's LQ.entry[idx] sat behind an aliasing barrier -O3 couldn't cross) — idea: cache.cc reload-elim. LEARNING: micro-opt vein exhausted; pivot to algorithmic (data-structure) opts -O3 can't do.
v18_0007 SKIPPED (feasibility) — B2 single-slot reg->producer flat array: reg-producer is actually a reg->ordered-list-of-all-in-flight-ROB-producers (ProducerList, keyed by uint8 0-255); get_latest_producer = windowed reverse-scan w/ fallback, remove_producer = tombstone-by-rob_index. Single slot can't reproduce -> would diverge checksum. REDIRECT -> B2' (replace the hash-MAP container with flat ProducerList[256] direct-index, ProducerList semantics unchanged).
v18_0008 | B2' reg_producers ankerl-map -> flat ProducerList[256] direct-index (ooo_cpu.h only; ProducerList window/tombstone semantics byte-unchanged) | checksum=140038115682937 | IPC=0.62178 | wall=PAIRED median 199.55s vs ckpt 201.87 = -2.32s/-1.15% (interleaved ×4, non-overlapping, checksum exact all 8) | PASS  [checkpoint dir: champsim_v18_optA_P11_B_B2]  (= 4th kept opt)
v18_0009 SKIPPED — no flattenable dense-key hot map left (census: mem_producers/address_to_lq/rob_maps/address_to_entries/mshr_map all SPARSE 64-bit keys; reg_producers already done by B2'). Map-flatten vein = one-shot, exhausted.
v19_0004 | predictors-#1: remove dead lpm_demand_compute_all on bypass read-miss path (cache.cc:588/606/623; ~140 discarded FP-div/miss) | checksum=140038115682937 | IPC=0.62178 | wall=PAIRED 175.1 vs 175.7 = -0.34% (noise-flat) | PASS — FULL-OUTPUT DIFF: every simulated stat byte-identical incl met[]-derived LPM/CPA/cAMT/MST; only host-timing (⏱/TPMI) + Last-compiled differ → PROVEN truly dead. Small win but correct dead-removal. [NEW CHECKPOINT champsim_v19_avx2_lpm]
v19_0003 ★★ | -mavx2 -mbmi2 build flags (Makefile.win CXXFLAGS+CFLAGS; binary was SSE-ONLY → now AVX2 256-bit, %ymm=4395 confirmed; NO -march/-mfma/-ffast-math) | checksum=140038115682937 | IPC=0.62178 | wall=PAIRED median 174.3s vs v19 198.8 = **-24.5s / -12.32%** (interleaved ×4, NON-overlapping, checksum exact all 8) | **PASS — BIGGEST WIN** [NEW CHECKPOINT: champsim_v19_avx2; ≈-16% cumulative vs frozen]
v19_0002 FAIL PAIRED +0.43% (median_ctz 208.5 vs median_v19 207.6, interleaved ×4, overlapping/noise, checksum exact all 8) — ctz-expand set.h 3 expand() variants __builtin_ctzll set-bit walk — cause: correct + checksum-exact, but COLD — dep-sets stay in small-array mode (card < SMALL_SIZE~13) so expand() rarely hits the bitset branch the opt targets. No benefit. KEEP v19.
v19_0001 FAIL PAIRED +1.94% (median_sv 212.64 vs median_v19 208.60, interleaved ×4, checksum exact all 8) — B2'' ProducerList std::vector->ankerl::svector<u16,8> — cause: (1) sizeof IDENTICAL 32B (svector inline 24B == std::vector header 24B → NO struct shrink; the ×256 array is the fixed cost, not the vec type); (2) deep-producer regs (old reserve(512) served them) THRASH svector<8> via repeated heap grow/realloc vs std::vector's one-time reserve. KEEP B2' (std::vector+reserve) in v19. Larger N only ADDS memory + may not fix thrash. Flat-array ~6KB/CPU over hashmap is trivial + NOT reducible via svector.

=== **v19 BANKED (committed + frozen)** === dir `champsim_v18_optA_P11_B_B2` = Opt-A+P11+Opt-B+B2'. VALIDATED PAIRED ×5 (drop-warmup): frozen-v18 median 207.9s vs stack median 198.9s = **−9.0s / −4.33%**, checksum 140038115682937 exact all 10 runs. FROZEN — never edit, only dup. Loop continues dup'ing this as v19_XXXX.

**VEIN STATUS (after 8 iters): SAFE veins mined out** — micro-opts captured by -O3 (W3-E/cache-hoists failed), map-flatten done (B2'), cache/DRAM flat (not the bottleneck; core=ooo_cpu is). 4 kept opts. Remaining = low-value-here (cache/DRAM) or RISKY (A1 SoA tag mirror — cache not hot; DRAM-GAP selection — bit-exact risk; __restrict — UB). → banking v19 at 4; re-validating cumulative.
